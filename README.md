# Snippetbox

https://hub.docker.com/repository/docker/smorenburg/snippetbox

```bash
docker build --platform linux/amd64 -t smorenburg/snippetbox:latest -t smorenburg/snippetbox:0.0.5 .
docker push smorenburg/snippetbox --all-tags
```